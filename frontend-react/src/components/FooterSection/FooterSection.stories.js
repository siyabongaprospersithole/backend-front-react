// Import the FooterSection component from the current directory.
import { FooterSection } from ".";

// Define the story for the FooterSection component.
export default {
  title: "Components/FooterSection", // Storybook title
  component: FooterSection, // Component to be rendered in Storybook
};

// Define the default arguments for the story.
export const Default = {
  args: {
    // You can define default arguments here if needed.
  },
};
