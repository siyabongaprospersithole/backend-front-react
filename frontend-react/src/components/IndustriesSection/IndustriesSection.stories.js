/*
  This file is responsible for configuring the IndustriesSection component.
  It sets up the component's story for documentation and testing purposes.

  The 'title' defines the component's story title, while 'component' specifies the component to showcase.
  You can use 'args' to customize the component's behavior when rendering it within the story.

  Example Usage:
  - To view the default IndustriesSection:
    <IndustriesSection />

  Metadata Props:
  - title: A string representing the story title.
  - component: The component to feature in the story.
*/

import { IndustriesSection } from ".";

export default {
  title: "Components/IndustriesSection",
  component: IndustriesSection,
};

export const Default = {
  args: {
  },
};
