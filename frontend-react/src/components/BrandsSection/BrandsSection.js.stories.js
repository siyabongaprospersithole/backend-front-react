import BrandsSection from "."; // Import the BrandsSection component

export default {
  title: "Components/BrandsSection", // Title of your Storybook story
  component: BrandsSection, // The component being showcased in Storybook
  argTypes: {
    // Define argTypes if needed
  },
};

export const Default = {
  args: {
    // Define default args if needed
  },
};
